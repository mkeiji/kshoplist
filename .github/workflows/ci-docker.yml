name: Docker

on:
    push:
        branches:
            - master
    pull_request:
        branches:
            - master

jobs:
    build:
        uses: mkeiji/kcicd/.github/workflows/ci-docker.yml@master
        with:
            build_context: ./
            dockerfile: ./Dockerfile
            release_branch: master
            build_args: WS_URL=kshoplistsrv.keijidev.com
        secrets: inherit
